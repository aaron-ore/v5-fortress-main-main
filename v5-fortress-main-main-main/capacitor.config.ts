import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.v4fortressmainmain',
  appName: 'v4-fortress-main-main',
  webDir: 'dist'
};

export default config;
